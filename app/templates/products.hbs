


<div class="flex border justify-between pl-12 pr-12 pt-4 pb-4 border-b-1 border-gray-200">
  <div class="stiker"></div> <LinkTo @route="cart"><Cart /></LinkTo>
</div>
<div class="mx-auto max-w-2xl px-4 py-16 sm:px-6 sm:py-24 lg:max-w-7xl lg:px-8">
 <div class="md:container md:mx-auto">


  <div class="grid grid-cols-1 gap-x-6 gap-y-10 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-3 xl:gap-x-8">
{{#if this.model}}
{{#each this.model as |product|}}
<Product @product={{product}} />
{{/each}}
    {{else}}
  loading

{{/if}}
  </div>
</div>
</div>